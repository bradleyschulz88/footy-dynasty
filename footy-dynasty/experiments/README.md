# Design experiments (non-production)

These JSX files are loaded by the static HTML prototypes in this folder’s parent directory (`Footy Dynasty Redesign.html`, `Footy Dynasty Mobile.html`, etc.). They are **not** part of the Vite app under `src/` (see `src/main.jsx`).

Keep prototype-only scripts here so the shipped React app stays isolated under `src/`.
